.media
  = link_to '#', class: 'pull-left' do
    = image_tag current_user.instagram_profile_picture,
      alt: current_user.instagram_username,
      class: 'media-object img-polaroid profile'
  .media-body
    %h4.media-heading== @#{current_user.instagram_username}
    %p This is some bio copy that needs to be pulled from Instagram and stored in the database.

%ul.thumbnails
  - instagram_client.user_recent_media.each do |item|
    %li.span3
      .thumbnail
        = image_tag item.images.low_resolution.url, alt: item.user.username
        %p= item.caption.text
